# AI Chat 计划状态复核

## 本次目标

- 对照当前代码库核查 `docs/ai-chat-refactor-plan.md` 中 M18 / Harness / Eval 相关状态。
- 将“已完成但无文件证据”的条目回退为真实进度，避免后续排期误判。

## 已完成

- 更新 `docs/ai-chat-refactor-plan.md` 顶部摘要：
  - 保留 M17 主线完成结论。
  - 明确 M18 目前只完成 Agent 后端测试扩充和最小 synthetic retrieval eval。
  - 标记 AI Chat 组件测试、mock E2E、Storybook、Memory eval、真实样本 eval、rerank、AI smoke CI、Tool Trace Replay 和 trace sink 持久化仍未完成。
- 校准 Phase 6 进度：
  - Agent 后端测试更新为现有 8 个测试文件覆盖范围。
  - 前端 AI 组件测试从完成回退为未做。
  - AI 模块 E2E 从完成回退为部分完成。
  - Storybook 和 AI smoke CI 从完成回退为未做。
- 校准 Phase 7 / Harness 进度：
  - RAG 保留 hybrid retrieval、query rewrite、multi-query、context packing、citation quality 和最小 `pnpm eval:retrieval`。
  - Rerank、真实/脱敏样本 eval、Memory eval、Tool Trace Replay、Agent golden set 和 CI smoke 标记为待补。
  - M18 里程碑状态从完成改为部分完成。

## 验证记录

- `Glob apps/web/src/components/ai-chat/**/*.{test,spec,stories}.{ts,tsx}`：未发现 AI Chat 组件测试或 stories。
- `Glob .github/workflows/*ai*`：未发现 AI smoke workflow。
- `Glob packages/ai/eval/**/*memory*`：未发现 Memory eval。
- `Glob packages/ai/eval/**/*real*`：未发现真实样本 eval。
- `Read package.json`：根脚本仅有 `eval:retrieval`，未发现 `ci:ai-smoke`、`eval:memory`、`eval:retrieval:real` 或 rerank 对比脚本。
- `Glob packages/ai/eval/**/*`：仅有 retrieval eval fixture / evaluator / runner。
- `Glob apps/web/src/lib/agent/__tests__/*.test.ts`：确认 Agent 后端测试文件已扩充。

## 已知缺口

- 本次只更新长期计划文档和进度记录，没有新增实现。
- 未运行 typecheck/build/lint，因为没有修改 TypeScript 代码。
- 后续若要重新标记 M18 完成，需要补齐对应文件、脚本和 CI 工作流。

## 下一步建议

1. 先做 Agent trace sink 持久化，为 Tool Trace Replay 提供数据源。
2. 补 Tool Trace Replay / Agent golden set，形成可回放的 Agent 质量基线。
3. 补 Memory eval 和真实/脱敏 retrieval eval，再决定是否引入 rerank。
4. 收口前端 Harness：AI Chat 组件测试、mock E2E、Storybook 和 `ci:ai-smoke`。
