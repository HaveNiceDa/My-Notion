# 2026-06-03 M25 Agent Memory Inbox Confirmation

## 本次目标

执行 `milestones/M25-agent-memory-inbox-confirmation.md`，将 Agent 记忆写入从 Tool 卡片单点确认升级为 pending proposal、Inbox 审核和确认式提交链路。

## 完成内容

- 新增 Convex Memory proposal 链路：
  - `packages/convex/agentMemories/logic/propose.ts`：创建 `pending_review` 记忆提议。
  - `packages/convex/agentMemories/logic/commit.ts`：将 pending proposal 提交为 `active`，并设置 `embeddingStatus=pending`。
  - `packages/convex/agentMemories/logic/reject.ts`：将 pending proposal 标记为 `rejected`。
  - `packages/convex/agentMemories/logic/listPending.ts`：查询当前用户待审核记忆。
  - `packages/convex/agentMemories/logic/shared.ts`：抽出写入字段归一化、默认值和内容 overlap 逻辑。
- 更新 `packages/convex/agentMemories/logic/create.ts`：
  - 复用共享 `buildMemoryPatch`，保持手动创建 active memory 的旧链路。
- 更新 `apps/web/src/lib/agent/tools/memory.ts`：
  - `memory_write` 默认 `dryRun=true` 时创建 `pending_review` proposal，不直接写 active memory。
  - 返回 `proposalId`、`proposalStatus`、`possibleDuplicateIds`、`possibleConflictIds`。
  - `dryRun=false` 仍保留直接写入兼容路径。
- 更新 `apps/web/src/lib/agent/tools/definitions.ts`：
  - 调整 `memory_write` 描述，明确默认进入 Memory Inbox。
  - 补充 `auto_extracted/system` source 和 `evidenceText` 参数。
- 更新 `apps/web/src/components/ai-chat/ToolCallCard.tsx`：
  - Tool 卡片保存按钮改为 `commitAgentMemory`。
  - 新增“保留在 Inbox”按钮，只更新 tool result 状态。
  - 取消按钮改为 `rejectAgentMemory`。
  - 展示可能重复/冲突提示。
- 更新 `packages/convex/chat/logic/updateToolResult.ts`：
  - 支持 `memoryWriteStatus=inbox`。
  - 持久化 `proposalId`。
- 更新 `/memories` 页面：
  - `apps/web/src/components/ai-chat/MemoryReviewPage.tsx` 增加最小 Inbox 区块。
  - Inbox 支持接受、编辑后接受、拒绝。
  - 接受后同步 memory 向量索引。
  - 展示 evidence、kind/category、possible duplicate/conflict 提示。
- 更新 i18n：
  - `packages/business/i18n/zh-CN.json`
  - `packages/business/i18n/en.json`
- 更新测试：
  - `apps/web/src/lib/agent/__tests__/tools.test.ts` 覆盖 `memory_write` 默认 pending proposal 行为。
- 运行 Convex codegen，同步 API 类型。
- 更新 `milestones/M25-agent-memory-inbox-confirmation.md` 状态为已完成。

## 验证命令与结果

```bash
pnpm --filter @notion/web exec convex codegen
```

- 结果：通过。

```bash
pnpm --filter @notion/web test -- src/lib/agent/__tests__/tools.test.ts
```

- 结果：通过。实际执行 Web Agent 相关 10 个测试文件，共 107 个测试通过。

```bash
pnpm --filter @notion/web typecheck
```

- 结果：通过。

```bash
pnpm --filter @notion/web lint
```

- 结果：通过。保留 7 个既有 warning，主要是 `<img>` 与既有 Hook dependency warning，本次无新增 error。

```bash
pnpm --filter @notion/web build
```

- 结果：通过。Sentry auth token warning 为本地构建环境既有提示，不影响构建。

```bash
pnpm ci:ai-smoke
```

- 结果：通过。AI Chat mock E2E 因本地缺少 `CLERK_SECRET_KEY` 被脚本按预期跳过。

```bash
pnpm test
```

- 结果：通过。22 个测试文件、118 个测试全部通过。

## 已知风险

- M25 的 duplicate/conflict 检测是最小规则版，只基于同 kind/category/scope 和内容 overlap，不做 LLM 裁决。
- Inbox 是 M25 最小形态，完整 Overview、Conflicts、Settings、Detail Drawer 属于 M26。
- `dryRun=false` 的直接 active 写入兼容路径仍保留，后续可在 M26/M27 根据产品策略逐步收紧。
- Tool 卡片与 Inbox 双入口以 Convex memory status 为最终状态；旧消息中没有 `proposalId` 的 tool result 仍需兼容展示。

## 下一步建议

1. 进入 M26，将 `/memories` 从当前最小 Inbox + Active 列表升级为 Memory Center。
2. M26 复用本阶段的 `pending_review`、`proposalId`、evidence、conflictsWith 数据。
3. 后续 M27 再引入自动提取，自动提取结果默认进入本阶段的 Inbox。

