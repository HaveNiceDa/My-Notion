# AI Smoke Workflow 去除新增 Secrets 依赖

## 本次目标

- 根据当前时间成本，移除 AI smoke GitHub Actions 对新增 GitHub Secrets 的依赖。
- 保留本地 `pnpm ci:ai-smoke` 能力，认证态 AI Chat mock E2E 仅作为本地可选验证。

## 已完成

- 更新 `.github/workflows/ai-smoke.yml`：
  - 移除 `AI_SMOKE_REQUIRE_E2E=1`。
  - 移除 Clerk、Convex、Qdrant、EdgeStore、LLM、Sentry 等 secrets 透传。
  - 移除 Playwright Chromium 安装步骤。
  - 增加 `AI_SMOKE_SKIP_E2E=1`，CI 仅跑 Web unit、Agent unit 和 `eval:retrieval`。
- 更新 `scripts/ci-ai-smoke.mjs`：
  - 将缺 `CLERK_SECRET_KEY` 时的提示改为本地运行指引，不再建议 CI 强制 E2E。
- 更新 `docs/ai-chat-refactor-plan.md`：
  - 将 AI smoke workflow 描述改为“无新增 secrets 依赖”。
  - 明确认证态 mock E2E 暂保留为本地可选验证，不作为当前 CI 门禁。

## 验证记录

- `AI_SMOKE_SKIP_E2E=1 pnpm ci:ai-smoke`
  - 通过。
  - Web unit：2 个测试文件，9 个用例通过。
  - Agent unit：8 个测试文件，89 个用例通过。
  - Retrieval eval：3 个 case 全通过，`recall@k=1.00`，`MRR=1.00`。
  - AI Chat mock E2E：按 `AI_SMOKE_SKIP_E2E=1` 预期跳过。
- `node --check scripts/ci-ai-smoke.mjs`
  - 通过。
- `GetDiagnostics`
  - `.github/workflows/ai-smoke.yml`、`scripts/ci-ai-smoke.mjs`、`docs/ai-chat-refactor-plan.md`、本 progress 文件均无诊断错误。

## 已知缺口

- CI 暂不覆盖认证态 AI Chat mock E2E。
- 若后续要恢复认证态 E2E 门禁，需要补齐 GitHub Secrets 后再移除 `AI_SMOKE_SKIP_E2E=1`。

## 下一步建议

1. 先以无 secrets 版 AI smoke 作为轻量 CI 门禁。
2. 继续补 Storybook，覆盖 AI Chat 主要组件的视觉回归。
3. 认证态 E2E 等后续有时间整理 Clerk/Convex secrets 后再接回 CI。
