# Phase 5 P1：CLI/MCP 发布文档收口

## 目标

将已闭环的 Agent 写文档能力产品化、文档化、可发布化：补充根 README 快速开始、发布检查清单，并同步更新架构计划。

## 已完成

- 更新根 `README.md`：
  - 增加 CLI / MCP Quick Start。
  - 覆盖 PAT 准备、CLI 登录、`docs create/update/import/export`、MCP STDIO 启动。
  - 补充 `pnpm e2e:cli:errors` 到常用验证命令。
  - 更新当前状态，明确 Agent 生态已完成 CLI / Skills / MCP STDIO 写文档主链路。
- 新增 `docs/my-notion-cli-release-checklist.md`：
  - 固化发布前命令：CLI typecheck/build、Convex codegen、Web tsc、CLI E2E、错误契约 E2E、MCP E2E、skills 同步。
  - 明确功能检查、错误契约、安全检查、skills 同步检查。
  - 明确 CLI 配置读取优先级和 config schema 向后兼容策略。
  - 提供 progress 发布结论模板。
- 新增 `packages/my-notion-skills/README.md`：
  - 修复根 README 与架构文档中的 Skills 目录入口。
  - 汇总 `my-notion-shared`、`my-notion-docs`、`my-notion-mcp` 的职责和同步命令。
- 更新 `docs/my-notion-cli-skills-architecture.md` 和 `.trae/documents/my-notion-cli-skills-architecture-plan.md`：
  - Phase 5 P1 中 README、release checklist、配置迁移策略标记完成。
  - 增加 release checklist 引用。

## 验证结果

- 本次仅修改 Markdown 文档，未修改运行时代码。
- 已检查 README、release checklist、架构文档和 `.trae` plan 的引用与状态。
- `pnpm sync:skills`：通过。
- 后续真正发布前需按 `docs/my-notion-cli-release-checklist.md` 执行完整命令。

## 当前状态

- Agent 写文档链路已进入发布收口阶段。
- 近期 P1 剩余项：
  - 为 `.trae/skills` 同步流程增加漂移校验。
  - 复查 MCP 写文档工具 dry-run 文案、`structuredContent` 和错误输出。

## 下一步建议

- 优先实现 `.trae/skills` 同步漂移校验，防止源 skill 与已安装 skill 内容不一致。
- 随后复查 MCP tool 输出体验，确认 Agent 默认安全、可解释。
