# 2026-06-03 M23 Agent Memory Schema Foundation

## 本次目标

执行 `milestones/M23-agent-memory-schema-foundation.md`，将 `agentMemories` 从 Memory MVP 单表扩展为后续 M24-M27 可复用的基础模型。

## 完成内容

- 扩展 `packages/convex/schemas/agentMemories.ts`：
  - 新增 `kind/category/scopeLevel/scopeKey`，为 instruction/semantic/episodic/procedural 分层和作用域治理打基础。
  - 新增 `summary/tags/evidence*`，支持后续 Memory Inbox 和 Memory Center 展示来源证据。
  - 新增 `importance/stability/privacy/reviewDueAt/lastUsedAt/usageCount`，支持后续治理和召回排序。
  - 新增 `embeddingStatus/embeddingUpdatedAt/embeddingError/embeddingRetryCount/nextEmbeddingRetryAt`，为后续移除读路径 upsert 和同步可观测打基础。
  - 扩展 `status` 为 `draft/pending_review/active/superseded/archived/rejected/deleted`。
  - 新增按 `kind/status`、`scope/status`、`embeddingStatus`、`reviewDueAt` 查询的索引。
- 新增 `packages/convex/agentMemories/model.ts`：
  - 提供 `memoryTypeValidator`、`memoryKindValidator`、`memoryStatusValidator` 等共享 validators。
  - 提供 `deriveMemoryDefaults`，将旧 `type` 映射到新 `kind/category/scope` 默认值。
  - 提供 `clampScore`，统一 confidence/importance 这类 0-1 分数字段。
- 更新 `packages/convex/agentMemories/logic/create.ts`：
  - 新建记忆时写入 M23 默认字段。
  - 保留旧 `type` 和现有写入主链路。
  - 支持可选 evidence、governance、embeddingStatus 字段。
- 更新 `packages/convex/agentMemories/logic/list.ts`：
  - 对旧数据派生 `kind/category/scope/importance/stability/privacy/usageCount/embeddingStatus` 默认值。
  - 返回新字段，同时保持旧消费者需要的 `id/type/content/source/reason/confidence/matchScore`。
- 更新 `packages/convex/agentMemories/logic/update.ts`：
  - 允许更新新模型字段中的安全子集。
  - 内容更新后默认将 `embeddingStatus` 设为 `pending`。
  - 对未传入的 evidence/tags/summary 不主动清空，避免旧 UI 更新误删新字段。
- 更新 `apps/web/src/lib/agent/tools/memory.ts`：
  - 扩展 `MemorySource` 类型，兼容 schema 新增的 `auto_extracted/system`。
- 新增 `packages/convex/__tests__/agent-memories-model.test.ts`：
  - 覆盖旧 `type` 到新 `kind/category/scope` 默认映射。
  - 覆盖 0-1 分数字段 clamp 行为。
- 运行 Convex codegen，同步 schema 类型。
- 更新 `milestones/M23-agent-memory-schema-foundation.md` 状态为已完成。

## 验证命令与结果

```bash
pnpm --filter @notion/web exec convex codegen
```

- 结果：通过。

```bash
pnpm test -- packages/convex/__tests__/agent-memories-model.test.ts
```

- 结果：通过。Vitest 实际执行根配置下 21 个测试文件，共 116 个测试全部通过。

```bash
pnpm --filter @notion/web typecheck
```

- 结果：通过。

```bash
pnpm --filter @notion/web lint
```

- 结果：通过。保留 7 个既有 warning，主要是 `<img>` 与既有 Hook dependency warning，本次未新增 lint error。

```bash
pnpm --filter @notion/web build
```

- 结果：通过。Sentry auth token warning 为本地构建环境既有提示，不影响构建。

## 已知风险

- 新字段在 schema 中保持 optional，以兼容线上旧数据；后续 M24-M27 消费时必须继续通过 defaults 处理旧记录。
- `embeddingStatus` 已落字段，但真正移除读路径 upsert 是 M24 范围。
- `pending_review` 状态已进入 schema，但 Inbox 与提议确认链路是 M25 范围。
- M23 暂未改造前端 Memory 页面，M26 会统一处理 Memory Center。

## 下一步建议

1. 执行 M24，基于 `kind/category/scope/embeddingStatus` 改造 `memory_search` 与运行时注入。
2. 在 M24 中移除 `retrieveRelevantMemories` 的读路径 upsert。
3. 执行 M25 前，复用本阶段的 `pending_review`、evidence 和 governance 字段实现 Inbox。

