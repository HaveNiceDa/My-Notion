# 2026-06-03 M26 Agent Memory Center UI

## 本次目标

执行 `milestones/M26-agent-memory-center-ui.md`，将 `/memories` 从简单记忆 CRUD/审核页升级为 Memory Center，让用户可以集中查看、审核、治理和调试 Agent 长期记忆。

## 完成内容

- 重构 `apps/web/src/components/ai-chat/MemoryReviewPage.tsx`：
  - 保留为兼容入口，实际渲染新的 `MemoryCenterPage`。
- 新增 Memory Center 组件目录：
  - `apps/web/src/components/ai-chat/memory/MemoryCenterPage.tsx`：统一组合 Overview、Inbox、Active、Conflicts、Settings 和 Detail Drawer。
  - `apps/web/src/components/ai-chat/memory/MemoryOverview.tsx`：展示 active、pending、sync failed、review due、sensitive、recently used 等核心指标。
  - `apps/web/src/components/ai-chat/memory/MemoryInbox.tsx`：承接 M25 `pending_review` 审核链路，支持接受、编辑后接受、拒绝和打开详情。
  - `apps/web/src/components/ai-chat/memory/MemoryActiveList.tsx`：展示 active 记忆，支持搜索、类型、kind、同步状态、隐私和排序过滤。
  - `apps/web/src/components/ai-chat/memory/MemoryConflicts.tsx`：展示冲突、替代关系和到期复核候选。
  - `apps/web/src/components/ai-chat/memory/MemorySettings.tsx`：提供后续自动记忆、敏感信息策略、默认 scope、TTL 等设置入口说明。
  - `apps/web/src/components/ai-chat/memory/MemoryDetailDrawer.tsx`：展示正文、summary、治理字段、同步/使用状态、evidence 和 relations。
  - `apps/web/src/components/ai-chat/memory/MemoryForm.tsx`：复用创建、编辑和审核编辑表单。
  - `apps/web/src/components/ai-chat/memory/types.ts`、`utils.ts`：集中维护 Memory Center 类型、过滤、指标和格式化逻辑。
- 更新 i18n：
  - `packages/business/i18n/zh-CN.json`
  - `packages/business/i18n/en.json`
- 更新组件测试：
  - `apps/web/src/components/ai-chat/ai-chat-components.test.ts` 增加 `MemoryOverview` 核心指标渲染测试。
- 修复 M26 新增 lint 问题：
  - `MemoryConflicts` 避免 render 期间直接调用 `Date.now()`，改为组件初始化时稳定取值。
- 更新 `milestones/M26-agent-memory-center-ui.md` 状态为已完成。

## 验证命令与结果

```bash
pnpm --filter @notion/web test -- src/components/ai-chat/ai-chat-components.test.ts
```

- 结果：通过。实际执行 Web AI Chat 相关 10 个测试文件，共 108 个测试通过。

```bash
pnpm --filter @notion/web typecheck
```

- 结果：通过。

```bash
pnpm --filter @notion/web lint
```

- 结果：通过。保留 7 个既有 warning，主要是 `<img>` 与既有 Hook dependency warning，本次无新增 error。

```bash
pnpm --filter @notion/web build
```

- 结果：通过。`/[locale]/memories` 已进入生产构建路由输出；Sentry auth token warning 为本地环境既有提示。

```bash
pnpm ci:ai-smoke
```

- 结果：通过。AI Chat mock E2E 因本地缺少 `CLERK_SECRET_KEY` 被脚本按预期跳过。

```bash
pnpm test
```

- 结果：通过。22 个测试文件、118 个测试全部通过。

## 已知风险

- M26 的 Conflicts 仍是基础治理视图，Archive / Keep Both 暂作为禁用占位，复杂合并策略留给后续阶段。
- Settings 当前是策略说明和入口，不做持久化，避免在缺少后端 schema 时制造假设置。
- Memory Detail Drawer 展示 evidence 和 relations，但尚未接入跳转到原消息、原文档或 trace replay 的深链。

## 下一步建议

1. 进入 M27，围绕自动提取、Eval、Trace 指标增强 Memory Center。
2. 将 M27 自动提取结果默认进入 M25/M26 已有 Inbox。
3. 后续为 Conflicts 增加真实 archive、supersede、merge 操作，并补 Tool/Trace 级别的使用历史。
