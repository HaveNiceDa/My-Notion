# Phase 1-4 E2E 验证与 Phase 5 计划细化

## 目标

验证 CLI / MCP 已落地的 Phase 1-4 主链路是否仍然可用，并将 Phase 5 的增强与工程加固从候选项细化为可执行待办任务。

## 验证结果

- `pnpm e2e:cli`：
  - 第一次运行失败在 `auth login` 阶段，错误为 `fetch failed`。
  - 脚本已执行 `[cleanup] revoke PAT token`。
  - 随后复跑通过。
  - 覆盖 CLI build、PAT 注入、`auth login`、`docs create/fetch/update/search`、`tokens revoke-current`、`auth logout`。
- `pnpm e2e:mcp`：通过。
  - 覆盖 CLI build、PAT 注入、MCP STDIO server 启动、`initialize`、`tools/list`、dry-run 写工具、真实 create/fetch/update/search 链路和 PAT cleanup。

## 已更新文档

- `docs/my-notion-cli-skills-architecture.md`
- `.trae/documents/my-notion-cli-skills-architecture-plan.md`

## Phase 5 细化内容

- P0：E2E 副作用清理。
- P0：机器 API 安全与可观测性。
- P1：文档导入导出。
- P1：Knowledge Base 能力。
- P1：Agent Ask / NDJSON Streaming。
- P2：HTTP MCP / OAuth 2.1。
- P2：发布、文档与开发体验。

## 当前结论

- Phase 1-4 主链路最终验证通过。
- 当前最优先的 Phase 5 任务是新增机器 API 文档归档能力，并让 CLI/MCP E2E 在 cleanup 阶段清理测试文档，避免 dev Convex 部署持续累积测试数据。
