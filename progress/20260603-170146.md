# 2026-06-03 Agent Memory M 级待办拆分

## 本次目标

基于 `docs/agent-memory-redesign-report.md`，将 Agent Memory 重构方案拆成多个可独立推进、前后衔接的 M 级技术待办文档。

## 完成内容

- 新增 `milestones/M23-agent-memory-schema-foundation.md`：
  - 聚焦 `agentMemories` schema 扩展、兼容层、作用域、证据链和 embedding sync 状态。
- 新增 `milestones/M24-agent-memory-retrieval-runtime.md`：
  - 聚焦纯读检索、`memory_search`、scope-aware ranking、compact instruction memory 和移除读路径 upsert。
- 新增 `milestones/M25-agent-memory-inbox-confirmation.md`：
  - 聚焦 `memory_propose` / `memory_commit`、`pending_review`、Inbox 和确认式提交链路。
- 新增 `milestones/M26-agent-memory-center-ui.md`：
  - 聚焦 `/memories` 从 CRUD 列表升级为 Memory Center，覆盖 Overview、Inbox、Active、Conflicts、Settings 和 Detail Drawer。
- 新增 `milestones/M27-agent-memory-eval-auto-extraction.md`：
  - 聚焦 Memory eval、trace 观测和受控自动提取，自动提取默认进入 Inbox。
- 更新 `milestones/README.md`：
  - 将 Agent Memory 重构报告和 M23-M27 加入阅读顺序。
  - 增加 Memory 重构待办路线说明，明确与 M20-M22 并行但不冲突。

## 验证结果

- 本次只新增和更新 Markdown 文档，没有改业务代码。
- 已对新增/更新的 Markdown 文档执行 diagnostics 检查，结果无错误。

## 已知风险

- M23-M27 是技术待办拆分，不代表代码已实现。
- M20-M22 仍保留为 MCP 扩展、韧性治理和 Harness 回补路线；执行排期需要后续根据主线优先级确认。
- M26 和 M27 依赖 M23-M25 数据与确认链路，不能跳过前置阶段直接做 UI 或自动提取。

## 下一步建议

1. 若优先解决 Memory 当前风险，先执行 M23 和 M24。
2. 若优先提升用户可控性，M23 后尽快执行 M25。
3. M26 应在 M25 之后做，避免 UI 先行但缺少 pending/evidence 数据。
4. M27 应最后做，确保自动提取不会污染 active memory。

