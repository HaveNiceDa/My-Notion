# AI Chat Refactor Plan 状态更新

## 本轮目标

- 对照当前代码状态更新 `docs/ai-chat-refactor-plan.md`。
- 明确 M17 已完成项、未完成项和下一步实施顺序。

## 已完成变更

- 将计划开头更新为当前状态：M17 已完成 Hybrid Retrieval 主线，`knowledge_search` 默认升级为混合检索。
- 更新 Phase 6 优先级排序，把当前重点调整为 Memory、Web Agent 写文档、Agent 可观测性和工具扩展。
- 更新 Phase 7 当前基线：
  - RAG 已支持 `retrieveKnowledge(options)`。
  - `balanced` 已走 semantic / keyword / metadata 三路召回和 RRF 融合。
  - `deep` 已接入 Query Rewrite 和 Multi-query Search。
  - Web Agent 工具仍只有 `knowledge_search`、`web_search`、`document_read`。
  - Memory 数据模型和 read/write tool 仍未实现。
- 为 Phase 7 Tool 清单新增“当前状态”列，区分 Web Agent 未完成项与 CLI/MCP 已完成能力。
- 更新建议实施顺序：Hybrid Retrieval 从待办中移除，下一步改为 Memory MVP、Document write dry-run、Agent 性能监控、Web extractor / document metadata search。

## 验证

- 静态核查：
  - `packages/ai/server/retrieval/index.ts`
  - `apps/web/src/lib/agent/tools/knowledge-search.ts`
  - `apps/web/src/lib/agent/tools/registry.ts`
  - `packages/my-notion-cli/src/mcp/server.ts`
- 本轮仅修改 Markdown 文档，未运行 Web typecheck/build/lint。

## 已知缺口

- `agentMemories` 数据模型未实现。
- Web Agent `memory_read` / `memory_write` 未实现。
- Web Agent `document_write` / `document_update` 未实现；CLI/MCP 侧已有 dry-run 写文档能力。
- Web Agent `web_extract`、`document_search`、`task_plan` 未实现。
- RAG 仍缺少 context packing、citation quality 和二阶段 rerank。

## 下一步建议

1. 先做 Memory MVP：Convex 结构化模型、read/write tool、删除/停用接口、确认写入契约。
2. 再做 Web Agent 文档写入 dry-run：复用 CLI/MCP create/update 安全语义。
3. 补 Agent trace：记录 ReAct 轮次、tool 耗时、LLM 延迟和降级原因。
