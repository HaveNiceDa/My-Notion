# 2026-07-01 模型白名单切换与 CLI/MCP 验证

## 目标

将 My-Notion 当前 AI 模型白名单切换为指定的四个 DashScope 模型，并验证 CLI/MCP 当前调用链路可用。

## 已完成

- 共享 AI 配置更新为：
  - `kimi-k2.7-code`
  - `qwen3.7-max-2026-06-08`
  - `qwen3.7-plus`
  - `qwen3.7-plus-2026-05-26`
- 默认模型改为 `kimi-k2.7-code`。
- Web AI 模型选择列表同步更新。
- 当前 Web/Mobile/services/AI 主线代码与测试中的旧模型引用已替换。
- 修复 `scripts/e2e-my-notion-cli.mjs`：E2E 显式设置 `MY_NOTION_PROD_API_URL`，符合 CLI prod endpoint 固定策略。

## 验证

- `pnpm --filter @notion/ai typecheck`：通过
- `pnpm --filter @notion/web typecheck`：通过
- `pnpm --filter @notion/mobile typecheck`：通过
- `pnpm --filter @notion/ai-service typecheck`：通过
- `pnpm exec vitest run packages/ai/__tests__/config.test.ts apps/web/src/components/ai-chat/ai-chat-components.test.ts apps/web/src/components/ai-chat/stream-client.test.ts`：通过；当前 Vitest 配置实际仅收集到 `packages/ai/__tests__/config.test.ts`
- `pnpm --filter @mynotion/cli typecheck`：通过
- `pnpm --filter @mynotion/mcp typecheck`：通过
- `pnpm --filter @mynotion/cli test`：通过，37 tests
- `pnpm --filter @mynotion/mcp test`：通过，1 test
- `pnpm --filter @mynotion/cli build`：通过
- `pnpm --filter @mynotion/mcp build`：通过
- `pnpm e2e:cli`：通过，真实 create/fetch/update/search/export/import/archive/revoke/logout 链路可用
- `pnpm e2e:mcp`：通过，STDIO server 工具链路可用
- `pnpm e2e:mcp:client`：通过，真实 MCP SDK client 可发现并调用工具
- `pnpm --filter @notion/web lint`：通过，保留 7 个既有 warning
- `pnpm --filter @notion/web build`：通过，Sentry auth token 缺失仅导致 sourcemap/release 上传 warning
- `pnpm --filter @notion/ai-service build`：通过

## 风险与后续

- 历史文档和 `my-notion-go/` 中仍保留旧模型记录，属于历史参考，不影响当前主线。
- Web 本地用户若 `localStorage.ai-model-id` 保存了旧模型，会自动回退到新的默认 `kimi-k2.7-code`。
