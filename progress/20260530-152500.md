# My-Notion CLI P0-P2 收口

## 背景

- 目标：修复 CLI P0 单测缺口，执行 P1 release checklist，并完成 P2 默认线上 API 地址体验优化。
- 默认线上 Machine API URL：`https://handsome-stoat-500.convex.site`。

## 变更

- 为 `@notion/my-notion-cli` 新增 Vitest 包级配置，保证 `pnpm --filter @notion/my-notion-cli test` 可直接运行。
- 新增 CLI 单测覆盖配置解析、输出格式、HTTP Client、`docs` 命令参数映射。
- `resolveApiUrl` 增加默认线上地址兜底；用户不指定 `--api-url`、`MY_NOTION_API_URL` 或本地配置时，默认连接当前线上部署。
- 同步更新 CLI usage、README、release checklist、my-notion Skills 文档，并同步 `.trae/skills`。

## 验证结果

- `pnpm --filter @notion/my-notion-cli test`：通过，4 个测试文件，19 个用例。
- `pnpm --filter @notion/my-notion-cli typecheck`：通过。
- `pnpm --filter @notion/my-notion-cli build`：通过。
- `pnpm --filter @notion/web exec convex codegen`：通过。
- `pnpm --filter @notion/web exec tsc --noEmit`：通过。
- `pnpm e2e:cli`：通过。
- `pnpm e2e:cli:errors`：通过。
- `pnpm e2e:mcp`：通过。
- `pnpm sync:skills`：通过。
- `pnpm sync:skills:check`：通过，diffCount 均为 0。
- 默认 API URL smoke：`auth status` 在无 API URL、无 token 时返回 `Missing API token`，确认 API URL 已由默认值兜底。

## 结论

- CLI 已具备可用的单元测试入口和核心低层覆盖。
- CLI / MCP / Skills release checklist 已通过。
- 当前线上部署成为默认 API URL；连接其他部署时仍可通过 `--api-url` 或 `MY_NOTION_API_URL` 覆盖。
