# AI Chat 前端测试补齐

## 本次目标

- 补齐 AI Chat 组件测试。
- 补齐 AI 模块 mock E2E 场景，覆盖对话、工具展示、写入确认和错误路径。

## 已完成

- 新增 `apps/web/src/components/ai-chat/ai-chat-components.test.ts`：
  - 覆盖 `MessageInput` 的模型展示、空输入禁用发送、发送中加载态。
  - 覆盖 `MessageList` 的 JSON 文本渲染、助手思考过程、加载状态。
  - 覆盖重复只读工具调用折叠展示。
  - 覆盖 `ToolCallCard` 对 `document_write` dry-run 结果的确认预览展示。
- 新增 `apps/web/src/components/ai-chat/stream-client.test.ts`：
  - 覆盖 `runAgentStream` 请求体构造。
  - 覆盖 `text-delta` / `reasoning-delta` / tool call / tool result / finish 事件分发。
  - 覆盖跨 chunk NDJSON 拆包。
  - 覆盖 stream `error` 事件和 429 `Retry-After` 友好错误。
- 更新 `apps/web/vitest.config.ts`：
  - 修正 `@` alias，使 `@/src/...` 在 Vitest 中能正确解析到 `apps/web`。
  - 补充 `@notion/business/utils` alias，支持 AI Chat 组件测试直接导入业务工具。
- 更新 `tests/web/api-auth.spec.ts`：
  - 追加 AI Chat mock E2E 用例。
  - 覆盖完整 mock 对话、只读工具展示、写入确认预览、stream error 和 429 retry-after。
- 更新 `docs/ai-chat-refactor-plan.md`：
  - 标记 AI Chat 组件/流客户端测试已完成。
  - 标记 AI Chat mock E2E 用例已补充，但本地执行仍依赖 Clerk 认证环境。

## 验证记录

- `pnpm --filter @notion/web test -- src/components/ai-chat/ai-chat-components.test.ts src/components/ai-chat/stream-client.test.ts`
  - 通过：10 个测试文件，98 个用例通过。
- `pnpm --filter @notion/web lint`
  - 通过：0 error，7 warning。
  - warning 均为既有 `img` / hook deps 规则提示，非本次新增测试错误。
- `CI= pnpm exec playwright test tests/web/api-auth.spec.ts --project=chromium-authenticated --reporter=list`
  - 未进入新增 E2E：`auth-setup` 因缺少 `CLERK_SECRET_KEY` 失败。
- `pnpm --filter @notion/web exec tsc --noEmit`
  - 未通过：`.next` 生成类型引用缺失的 `src/app/[locale]/monitoring/ai-chat-mock/page.js`，与本次新增测试无关。
- `GetDiagnostics`
  - 新增/修改文件无 TypeScript 诊断错误；`api-auth.spec.ts` 仅有 cSpell info。

## 已知缺口

- AI Chat mock E2E 用例已实现，但需要具备 Clerk 测试密钥和 auth setup 的环境才能完整执行。
- 组件测试当前使用 `react-dom/server` 做渲染级断言，未引入 Testing Library，因此不覆盖真实 DOM 点击交互。
- Storybook、Memory eval、真实样本 retrieval eval、AI smoke CI、Tool Trace Replay 和 trace sink 持久化仍待补。

## 下一步建议

1. 在具备 `CLERK_SECRET_KEY` 的环境运行认证态 Playwright，确认新增 mock E2E。
2. 若后续允许增加测试依赖，可引入 Testing Library + jsdom，补真实 DOM 交互测试。
3. 继续补 Storybook 和 `ci:ai-smoke`，把前端 Harness 接入 CI。
