# 修复 i18n + Qdrant 本地配置 + RAG stream 容错

## 完成的改动

### 1. 修复 i18n 缺失 key
- `packages/business/i18n/zh-CN.json` — 新增 `AI.generatingResponse: "正在生成响应..."`
- `packages/business/i18n/en.json` — 新增 `AI.generatingResponse: "Generating response..."`

### 2. 配置本地 Qdrant Docker
- `apps/web/.env.local` — `NEXT_PUBLIC_QDRANT_URL` 从远程 Qdrant 改为 `http://localhost:6333`
- `NEXT_PUBLIC_QDRANT_API_KEY` 清空（本地无需 API Key）
- 复用 my-notion-go 的 `my-notion-go-qdrant` 容器（`qdrant/qdrant:v1.12.4`，端口 6333）
- my-notion 的 collection 命名规则为 `user_${userId}_knowledge_base`，首次使用时自动创建

### 3. RAG stream Clerk token 容错
- `apps/web/src/app/api/rag-stream/route.ts` — `getStreamToken` 调用增加 try-catch
- Clerk token 获取失败时不再导致 500，而是降级为无 token 模式（Convex 公开数据仍可用）

## 验证

- Qdrant 健康检查: `curl http://localhost:6333/healthz` → `healthz check passed`
- 现有 collection: `my_notion_go_chunks`（Go 版专用，my-notion 会自动创建新的）

## 已知缺口

- RAG stream 500 的根本原因可能是 Clerk JWT template 配置或网络问题，需要确认 Clerk Dashboard 中 `convex` template 是否正确配置
- 本地 Qdrant 数据为空，需要通过 `/api/rag-documents` 的 `initKnowledgeBaseVectorStore` 初始化
