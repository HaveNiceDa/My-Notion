# AI Smoke Workflow 接入

## 本次目标

- 新增 `.github/workflows/ai-smoke.yml`，把 `pnpm ci:ai-smoke` 接入 GitHub Actions。

## 已完成

- 新增 `.github/workflows/ai-smoke.yml`：
  - 触发条件：`push`、`pull_request`、`workflow_dispatch`。
  - 并发控制：同一 ref 只保留最新一次 `ai-smoke`。
  - 安装依赖：`pnpm install --frozen-lockfile`。
  - 安装浏览器：`pnpm exec playwright install --with-deps chromium`。
  - 执行入口：`pnpm ci:ai-smoke`。
  - 严格 E2E：设置 `AI_SMOKE_REQUIRE_E2E=1`，CI secrets 不完整时会硬失败。
  - 透传现有 Playwright / Web dev server 需要的 Clerk、Convex、Qdrant、EdgeStore、LLM、Sentry secrets。
  - 上传 `test-results/` 和 `playwright-report/` 作为失败分析 artifact。
- 更新 `docs/ai-chat-refactor-plan.md`：
  - `3.6 CI 集成 AI 测试` 标记为完成。
  - Harness 当前状态增加 AI smoke GitHub Actions。
  - 后续缺口保留 Storybook、Memory eval、真实样本 eval、Tool Trace Replay 和 trace sink。

## 验证记录

- `GetDiagnostics`
  - `.github/workflows/ai-smoke.yml`：无诊断错误。
  - `docs/ai-chat-refactor-plan.md`：无诊断错误。
- 未在本地执行 GitHub Actions workflow；本地已在上一轮验证 `pnpm ci:ai-smoke` 可执行，并按预期在缺 `CLERK_SECRET_KEY` 时跳过 E2E。

## 已知缺口

- workflow 在 CI 中会强制要求 `CLERK_SECRET_KEY`、`E2E_CLERK_USER_EMAIL`、`E2E_CLERK_USER_PASSWORD` 等 secrets；缺失时会失败，这是预期行为。
- 尚未在真实 GitHub Actions 环境验证 secrets 是否完整、auth setup 是否可成功创建会话。

## 下一步建议

1. 在 GitHub Actions 手动触发 `AI Smoke`，验证 secrets 配置和认证态 mock E2E。
2. 若 workflow 首次失败，优先检查 Clerk / Convex / `NEXT_PUBLIC_APP_URL` 相关 secrets。
3. 继续补 Storybook，或转向 Agent trace sink，为 Tool Trace Replay 做数据源。
