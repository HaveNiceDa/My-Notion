# My-Notion CLI 用户使用说明落地

## 目标

将面向使用者的 My-Notion CLI 完整流程说明整理为 Markdown 文件，放入 CLI 包内，方便后续发布、阅读和维护。

## 已完成

- 新增 `packages/my-notion-cli/docs/usage.md`。
- 文档覆盖：
  - 准备工作与 PAT 要求。
  - CLI 安装、开发运行和构建运行方式。
  - `auth login/status/logout`。
  - `docs create/fetch/search/list/update/export/import/archive`。
  - `tokens revoke-current`。
  - 输出格式选择。
  - 环境变量配置。
  - 推荐完整流程。
  - 安全注意事项。
  - 常见错误码说明。

## 验证

- `GetDiagnostics`：无诊断错误。
- 已确认 `packages/my-notion-cli/docs/usage.md` 存在。
- 已确认文档包含标题、推荐完整流程和常见错误章节。

## 后续建议

- 如准备发布 CLI 包，可在 `packages/my-notion-cli/README.md` 或根 README 中链接到 `docs/usage.md`。
