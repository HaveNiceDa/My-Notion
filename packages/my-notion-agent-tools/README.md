# @mynotion/agent-tools

Shared My-Notion Agent tool contracts, Machine API client, dry-run document tool logic, and MCP readme/manifest helpers.

This package is intentionally transport-agnostic. It does not depend on the MCP SDK or CLI output formatting.
